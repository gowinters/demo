name: "[Auto] CI Checks execution"
run-name: "Auto: CI checks for merge ${{ github.event.pull_request.title }} ⭐ triggered by ${{github.triggering_actor}}"

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - release
      - develop

jobs:
  sonarcloud:
    name: Build and analyze
    runs-on: windows-latest
    steps:
      - name: Set up JDK 17
        run: echo "This will run"
